import { Currency } from '@/lib/types';

interface FormProps {
  currencies: Currency[]
}

export default function NewWalletForm({currencies}: FormProps) {
  return (
    <div>
      NewWalletForm
    </div>
  );
}